<local:ForegroundWindow
    x:Class="Vimium.Views.SelectionModeOverlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Vimium.Views"
    WindowStyle="None"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    AllowsTransparency="True"
    Topmost="True"
    ShowActivated="False"
    Loaded="SelectionModeOverlayView_OnLoaded"
    Closed="SelectionModeOverlayView_OnClosed">
    <local:ForegroundWindow.Background>
        <SolidColorBrush Color="Transparent" />
    </local:ForegroundWindow.Background>
    <Grid>
        <Canvas x:Name="layoutCanvas" Background="Transparent">
            <!-- Text rendering and overlays managed in code-behind -->
        </Canvas>
        <!-- Copy confirmation toast -->
        <Border x:Name="CopiedToast"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Background="#333333"
                CornerRadius="4"
                Padding="16,8"
                Margin="0,0,0,40"
                Visibility="Collapsed">
            <Border.Resources>
                <Storyboard x:Key="FadeOutStoryboard">
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="1.0" To="0.0" Duration="0:0:0.5"
                                     BeginTime="0:0:0.3" />
                </Storyboard>
            </Border.Resources>
            <TextBlock Text="Copied!"
                       Foreground="#FFFFFF"
                       FontSize="14"
                       FontWeight="Bold"
                       FontFamily="Segoe UI, Helvetica, Arial" />
        </Border>
    </Grid>
</local:ForegroundWindow>
